# Copy to .env and set on your host too. Leave it unset and the contact form
# falls back to opening the visitor's mail client, which still works but
# converts far worse than a real submit.
#
# Formspree, Basin and Web3Forms all accept a plain JSON POST like this one.
VITE_FORM_ENDPOINT=
